[local]
localhost ansible_connection=local

[control_plane]
# Populated at runtime by multipass_vms role

[workers]
# Populated at runtime by multipass_vms role

[k8s_cluster:children]
control_plane
workers
