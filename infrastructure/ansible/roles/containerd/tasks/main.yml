---
# ---------------------------------------------------------------------------
# 1. Install containerd
# ---------------------------------------------------------------------------
- name: Install containerd
  ansible.builtin.apt:
    name: containerd
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [cluster]

# ---------------------------------------------------------------------------
# 2. Generate default config and enable SystemdCgroup
# ---------------------------------------------------------------------------
- name: Ensure /etc/containerd directory exists
  ansible.builtin.file:
    path: /etc/containerd
    state: directory
    mode: "0755"
  tags: [cluster]

- name: Check if containerd config already exists
  ansible.builtin.stat:
    path: /etc/containerd/config.toml
  register: containerd_config_stat
  tags: [cluster]

- name: Generate default containerd config
  ansible.builtin.shell: containerd config default > /etc/containerd/config.toml
  when: not containerd_config_stat.stat.exists
  changed_when: not containerd_config_stat.stat.exists
  notify: Restart Containerd
  tags: [cluster]

- name: Enable SystemdCgroup in containerd config
  ansible.builtin.replace:
    path: /etc/containerd/config.toml
    regexp: 'SystemdCgroup = false'
    replace: 'SystemdCgroup = true'
  notify: Restart Containerd
  tags: [cluster]

# containerd 2.x default config generates a colon-separated config_path
# (e.g. '/etc/containerd/certs.d:/etc/docker/certs.d'). The CRI plugin fails
# to resolve the registry hostname when a colon appears in both the path
# separator and the host:port directory name, causing it to silently fall back
# to HTTPS and ignore hosts.toml. Normalise to a single path.
- name: Fix config_path to single directory so hosts.toml is found
  ansible.builtin.replace:
    path: /etc/containerd/config.toml
    regexp: "config_path = '/etc/containerd/certs\\.d:[^']*'"
    replace: "config_path = '/etc/containerd/certs.d'"
  notify: Restart Containerd
  tags: [cluster]

# ---------------------------------------------------------------------------
# 3. Configure registry for plain-HTTP pulls
#    Uses the per-registry hosts.toml approach (containerd v1.6+)
# ---------------------------------------------------------------------------
- name: Create containerd certs.d directory for registry
  ansible.builtin.file:
    path: "/etc/containerd/certs.d/{{ hostvars['localhost']['host_registry_host_ip'] }}:{{ registry_port }}"
    state: directory
    mode: "0755"
  tags: [cluster]

- name: Template registry hosts.toml for containerd
  ansible.builtin.template:
    src: hosts.toml.j2
    dest: "/etc/containerd/certs.d/{{ hostvars['localhost']['host_registry_host_ip'] }}:{{ registry_port }}/hosts.toml"
    mode: "0644"
  notify: Restart Containerd
  tags: [cluster]

# ---------------------------------------------------------------------------
# 4. Enable and start containerd
# ---------------------------------------------------------------------------
- name: Enable and start containerd
  ansible.builtin.systemd:
    name: containerd
    enabled: true
    state: started
  tags: [cluster]
