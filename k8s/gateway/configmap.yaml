apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: notifications
data:
  PORT: "3000"
  NODE_ENV: "production"
  KAFKA_BROKERS: "kafka:9092"
  SCHEMA_REGISTRY_URL: "http://schema-registry:8081"
